*AI-плейлисты снова запускаются быстро* ⚡️

- Перевели дефолтный CheapVibeCode на `deepseek-v4-flash` и быстрый RU endpoint.
- Старый адрес провайдера теперь автоматически исправляется без ручной правки базы.
- Убрали повторные запросы для постоянных ошибок и ограничили зависший AI-вызов 45 секундами.
- Подправили оптическое выравнивание и размер точечного логотипа в шапке.
