*Чистка репо*

- Убраны из трекинга: сессии `.opencode/opencode-loop/` (22 файла), `deploy/.opencode/`, `miniapp/tsconfig.tsbuildinfo`
- Удалён дублирующий `CLAUDE.md` (был подмножеством `AGENTS.md`)
- Убраны из Git: `NOTES.md`, `progress.md` (рабочие заметки)
- Добавлено в `.gitignore`: `.opencode/opencode-loop/`, `docs/superpowers/`, `CLAUDE-SECURITY-*/`, `.commandcode/taste/`, `.ignore`, `.mcp.json`, `opencode.json`, `install.log`
- Упрощён `.gitignore`: `data/*.sqlite` → `data/`, убраны дубли

В репо осталось 796 трековых файлов (было 823).
