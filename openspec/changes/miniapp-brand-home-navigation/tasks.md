# Mini App brand home navigation — Tasks

## 1. Header behavior

- [x] 1.1 Convert the header brand to an accessible button and wire it to the
  default prompt screen.
- [x] 1.2 Reset the prompt state, errors, and open overlays when returning home.
- [x] 1.3 Preserve the current header appearance and keyboard focus visibility.

## 2. Verification

- [x] 2.1 Run `bun run typecheck`.
- [x] 2.2 Run `bun run build:miniapp`.
- [x] 2.3 Manually verify from another screen/tab and from the prompt search mode
  that clicking the brand restores the default prompt screen.
