# refine-miniapp-material-hierarchy — Tasks

## 1. Аналитический baseline

- [x] 1.1 Зафиксировать референс 368×582 и отдельно описать первый экран,
  режимы AI/Поиск, prompt field, rail карточек и bottom navigation.
- [x] 1.2 Составить матрицу строк: кириллица, латиница, транслит, смешанный
  запрос, длинное название, emoji и uppercase; указать ожидаемое поведение
  без clipping/overflow.
- [x] 1.3 Добавить screen-аудит для главной, поиска, плеера, lyrics, AI loading,
  магазина, плейлистов и медиатеки с оценкой slop и решением `remove`,
  `simplify`, `replace` или `intentional exception`.
- [x] 1.4 Повторно прогнать визуальный аудит в dark/light темах на 320, 375,
  390 и 430px; не считать чистый detector достаточным доказательством качества.

## 2. Типографика и иерархия

- [x] 2.1 Выбрать и зафиксировать реально доступную гарнитуру с Cyrillic + Latin
  coverage; проверить, что fallback не меняет характер текста внутри одной
  строки.
- [x] 2.2 Поднять заметность бренда и секционных заголовков, сохранив body
  text не ниже 16px и meta text не ниже 12px.
- [x] 2.3 Упростить повторяющиеся labels и привести spacing к 8dp-ритму без
  изменения пользовательского смысла русских текстов.
- [x] 2.4 Проверить, что в интерфейсе не преобладает вес 700+: оставить
  Regular/Medium основным, Semibold — для иерархии, Bold — только для редких
  акцентов.

## 3. Material-подобные поверхности и иконки

- [x] 3.1 Убрать лишние nested pills/glass treatments; документировать
  оставшиеся исключения для prompt field, media artwork, floating player и dock.
- [x] 3.2 Добавить `@mui/icons-material` точечной зависимостью и заменить
  Lucide-иконки на Material UI icons с единым mapping для поиска, AI, музыки,
  магазина, профиля и админки.
- [x] 3.3 Не использовать barrel-импорты и не добавлять весь `@mui/material`;
  проверить размер miniapp bundle после миграции.
- [x] 3.4 Проверить touch targets 44×44px, focus-visible и контраст активного
  таба в обеих темах.

## 4. Motion

- [x] 4.1 Добавить единый pressed-state для обычных и icon-only кнопок, табов и
  карточек: 140–180ms, tonal background/color, `translateY(1px)`/`scale(.98)`
  без layout shift.
- [x] 4.2 Добавить/проверить transition активного таба и переключателя AI/Поиск;
  активное состояние должно подтверждаться не только цветом.
- [x] 4.3 Проверить touch/pointer поведение, `focus-visible` и
  `prefers-reduced-motion`; motion сохранить отдельно для генерации, playback
  и progress.
- [x] 4.4 Проверить, что анимация не срабатывает на disabled-кнопках и не
  конфликтует с системным tap highlight Telegram WebView.

## 5. Screen-specific cleanup

- [x] 5.1 Главная: заменить AI-landing headline на спокойный продуктовый
  заголовок без окрашивания одного слова; объединить rail карточек и убрать
  лишний nested rounded treatment.
- [x] 5.2 Поиск: проверить вариант без отдельного крупного переключателя на
  экране поиска; привести названия разделов к естественным и фактически
  подтверждённым данным.
- [x] 5.3 Плеер: ограничить play/pause 52–56px, собрать like/lyrics в одну
  action-группу, добавить elapsed/total time и увеличить artwork примерно на
  15–20% без потери safe area.
- [x] 5.4 Lyrics: снизить weight и line-height, оставить активную строку
  белой, остальные — серыми.
- [x] 5.5 AI loading: заменить большую loading-карточку на chip запроса,
  компактную статусную строку и постепенное появление первых результатов.
- [x] 5.6 Магазин: представить тарифы как однозначный выбор продукта и цены;
  рубли сделать primary, Telegram Stars — secondary, CTA оставить одну.
- [x] 5.7 Плейлисты и медиатека: убрать dashboard-like внешние рамки, разделить
  «Треки», «Плейлисты», «Загрузки» и оставить одну primary action на строку.
- [x] 5.8 Уточнение запроса: убрать внешний большой card и приветственное
  дублирование; представить варианты как обычные строки с понятным `→`.
- [x] 5.9 Desktop Create: привести AI/Поиск и input к одному dominant action,
  не размещать рядом два визуально равных паттерна.
- [x] 5.10 Desktop player: добавить previous, elapsed/total time, уменьшить
  artwork на 10–15% и сохранить очередь вторичным уровнем.
- [x] 5.11 Desktop search: ограничить content до 720–800px или использовать
  правую часть под related results/preview; selected result сделать тонким
  border/tint без большой коричневой карточки.
- [x] 5.12 Results desktop/mobile: заменить `ПЛЕЙЛИСТ` на `Плейлист`, снизить
  H1 до 600–650 и привести размеры к Results typography baseline.
- [x] 5.13 Results mobile: проверить вариант artwork 112–128px или layout
  artwork слева/title справа; не оставлять маленькую обложку рядом с тяжёлым H1.
- [x] 5.14 Results: показывать карандаш только на hover/focus или открывать
  rename по tap на название; `+ Новый плейлист` вынести в «Музыка»/меню.
- [x] 5.15 Results actions: унифицировать Save/Share/Download либо явно
  подтвердить Download как primary; не позволять orange CTA конкурировать с H1.
- [x] 5.16 Results extend: заменить copy на `Добавить по описанию` или
  `Опишите, что добавить`, уменьшить `+` до 36–40px.
- [x] 5.17 Track rows: heart показывать только для saved/active состояний,
  остальные действия открыть через `⋯`/tap; сохранить один понятный playing
  indicator.
- [x] 5.18 Mobile chrome: развести mini-player и bottom navigation полноширинным
  player-слоем либо 8–12px боковыми отступами и стабильным gap.
- [x] 5.19 Playlist artwork: проверить, что collage не выглядит fallback;
  выбрать одну обложку или осмысленно разные изображения.

## 6. Copy и product tone

- [x] 6.1 Провести ревизию copy по таблице в design.md; заменять только там,
  где новая формулировка точнее и естественнее.
- [x] 6.2 Зафиксировать позиционирование: музыкальный сервис с AI-подбором,
  а не AI-демо с музыкальными карточками.
- [x] 6.3 Проверить словарь desktop и mobile: не использовать «Уточним
  детали», если экран фактически просит выбрать тип плейлиста; не дублировать
  вопрос в заголовке, приветствии и CTA.

## 7. Названия и бренд

- [x] 7.1 Обновить инструкции агенту и schema `finalize_playlist` для коротких
  естественных названий в языке запроса.
- [x] 7.2 Добавить безопасный fallback имени из запроса вместо `Playlist` и
  покрыть его тестами для кириллицы, латиницы, транслита и пустого ввода.
- [x] 7.3 Проверить ручное переименование и длинные названия в ResultsScreen;
  сохранить переносы/ellipsis без выхода за viewport.
- [x] 7.4 Увеличить logo/title в header после утверждения baseline и проверить
  узкие экраны и длинный `headerTitle`.
- [x] 7.5 Проверить, что оранжевый не используется одновременно как headline,
  selection, loader, icon и active state; оставить только action/state cases.

## 8. Verification и handoff

- [x] 8.1 После реализации запустить `bun run typecheck` и `bun test` для
  server/shared logic изменений.
- [x] 8.2 После реализации запустить `bun run build:miniapp` для UI изменений.
- [x] 8.3 Провести ручную проверку screenshot/Telegram WebView: dark/light,
  Cyrillic/Latin, pressed/focus/disabled/loading/error и отсутствие
  горизонтального overflow.
